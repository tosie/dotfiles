# Enable history in iex.
export ERL_AFLAGS="-kernel shell_history enabled -kernel shell_history_file_bytes 1024000 -kernel shell_history_path '\"$HOME/.erlang-history\"'"

# Use VSCode as the default editor for ecto migrations.
# export ECTO_EDITOR="code -w"
